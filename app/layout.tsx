import type { Metadata } from "next";
import { Inter } from "next/font/google";
import "./globals.css";

const inter = Inter({ 
  subsets: ["latin"],
  variable: '--font-inter',
});

export const metadata: Metadata = {
  title: "TrueCost - Anti-Dark Pattern Fintech",
  description: "A value-sensitive design intervention that helps users visualize the real cost of Buy Now, Pay Later decisions.",
  keywords: ["value sensitive design", "fintech", "BNPL", "ethical design", "financial literacy"],
};

export default function RootLayout({
  children,
}: Readonly<{
  children: React.ReactNode;
}>) {
  return (
    <html lang="en">
      <body className={`${inter.className} antialiased`}>
        {children}
      </body>
    </html>
  );
}
